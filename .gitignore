# ── Python ─────────────────────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# ── OS / Editor ────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/
*.swp

# ── 임시 산출물 ────────────────────────────────────────
*.tmp
*.bak
_scratch/
run.log
run.err
_preview_*.png

# ── 공개하지 않는 것 ───────────────────────────────────
# v1(HBM KGD Test Strategy) 원본 문서 일체.
# 2026.08.11 원점 재검토로 문제정의가 완전히 교체됐고, 문서 대부분이
# 일정·진행 관리 성격이라 공개 저장소의 범위 밖이다. 로컬에만 보관한다.
archive/v1_kgd/

# 주의: data/ 와 figures/ 는 의도적으로 커밋한다.
# 이 저장소의 결론은 로그와 CSV 로 재현 가능해야 하며,
# 모든 산출물은 src/run_all.py 로 다시 만들 수 있다.
